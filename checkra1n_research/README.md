# checkra1n_research

这个目录用于放与 checkm8 / checkra1n 相关的“研究依赖”：

- `pongoOS/`：pongoOS 源码（用于构建 `pongoterm`、参考符号与结构）
- `ipwndfu/`：ipwndfu（可选）

它们在本仓库里以 **git submodule** 的方式管理。

初始化（只需要做一次）：

```bash
git submodule update --init --recursive
```
